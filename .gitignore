# Environment variables (contains API keys and account info)
.env
.env.notes

# Local database (contains user data)
processed_emails.db

# Lock file (prevents concurrent execution)
.fastmail2ynab.lock

# Python bytecode cache
__pycache__/
*.pyc

# Claude Code files
.claude/
